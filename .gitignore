node_modules/

# Playwright output. Traces, videos and screenshots are only written on failure
# and are large; the report is regenerated by `npm run test:report`.
test-results/
playwright-report/
blob-report/
.playwright/
